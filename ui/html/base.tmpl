{{define "base"}}
    <!doctype html>
    <html lang="en">
    <head>
        <meta charset="utf-8"/>
        <title>Alex Brunn | {{template "title" .}}</title>
        <link rel='stylesheet' href='/static/css/styles.css'>
        <link rel='shortcut icon' href='/static/public/img/favicon.ico' type='image/x-icon'>
    </head>
    <body>
        <div class="layout">
            <header>
                <div class="container">
                    {{template "header" .}}
                </div>
            </header>
            <main>
                <div class="container">
                    {{template "main" .}}
                </div>
            </main>
            <footer>
                {{template "footer" .}}
            </footer>
        </div>
    </body>
    </html>
{{end}}